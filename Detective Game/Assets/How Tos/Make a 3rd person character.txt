You will need to have a humanoid marked FBX Model. Invector has a default
Go to the top bar and click invector -> Basic Locomotion -> Create... 
Then select the FBX model you want rest will happen for you